version: 1
mission: "Deploy any VLA model to any edge hardware. One command."

# ── CURRENT FOCUS (updated 2026-04-19 post-LIBERO run) ─────────────
# LIBERO-10 ran end-to-end for the first time (scripts/modal_libero_monolithic.py).
# Result: 0/10 = 0.0% task success. Root-cause hypothesis: adapter `vlm=off`
# — fine-tune was trained with language conditioning, adapter runs blind.
# See reflex_context/06_experiments/task_success_results.md.
#
# The original current_focus (task-success-benchmark + gr00t-vlm-conditioning)
# is now split into more actionable Phase 2 sub-goals below.
current_focus:
  - id: vla-eval-adapter-vlm-on
    why: "The fastest signal path to a non-zero LIBERO-10 number. Adapter currently routes images + state to the expert but feeds dummy language; fine-tune expects full VLM-conditioned tokens. Enabling this validates the hypothesis OR localizes the bug to something deeper."
    eta: "0.5-1 day"
  - id: libero-via-monolithic-onnx
    why: "The real customer-path task-success test. Currently LIBERO uses REFLEX_NATIVE=1 (PyTorch path). The monolithic ONNX (what customers ship) needs to be plumbed into the adapter + re-measured. Depends on vla-eval-adapter-vlm-on passing first."
    eta: "1-2 days (blocked on vla-eval-adapter-vlm-on)"
  - id: libero-preprocessing-audit
    why: "Contingency if vlm-on still yields 0%. Inspect preprocessing / camera-keying / state normalization differences between SmolVLA LIBERO fine-tune's training-time pipeline and our adapter's runtime."
    eta: "1-3 days (only if needed)"
  - id: gr00t-vlm-conditioning
    why: "Completes GR00T to be deployable. DiT stack is cos=1.0 but VLM is zero-stubbed. Parallel track — doesn't depend on LIBERO items."
    eta: "1-2 days"

goals:
  # ── CRITICAL (weight 10) ──────────────────────────────────────────

  - id: vlm-prefix-encoder
    description: "VLM backbone exports as real SigLIP+SmolLM2 ONNX (not stub) with vlm_kv_dim=960 so /act returns task-conditioned actions"
    check: "test -f src/reflex/exporters/vlm_prefix_exporter.py && grep -q 'AutoModel\\|from_pretrained' src/reflex/exporters/vlm_prefix_exporter.py 2>/dev/null && ! grep -q 'AdaptiveAvgPool2d' src/reflex/exporters/vlm_prefix_exporter.py 2>/dev/null"
    weight: 10

  - id: text-embedder-onnx
    description: "reflex export produces text_embedder.onnx (real SmolLM2 embed_tokens) so text encoding uses real embeddings not seeded-random fallback"
    check: "grep -q 'text_embedder' src/reflex/exporters/vlm_prefix_exporter.py 2>/dev/null && grep -q 'embed_tokens' src/reflex/exporters/vlm_prefix_exporter.py 2>/dev/null"
    weight: 10

  # ── HIGH (weight 8-9) ─────────────────────────────────────────────

  - id: distill-dmpo
    description: "reflex distill implements DMPO one-step generation (no teacher) targeting 1000+ Hz on consumer GPUs"
    check: "test -f src/reflex/distill/dmpo.py && .venv/bin/python -c 'from reflex.distill.dmpo import DMPOTrainer' 2>/dev/null"
    weight: 9

  - id: multi-model-native-parity
    description: "PyTorch-native path produces cos=1.0000 vs the reference PyTorch policy for all 4 flow-matching VLAs (SmolVLA, pi0, pi0.5, GR00T). Decomposition strategy differs per model: SmolVLA needs Llama-family RMSNorm swap; pi0 needs no swap (PiGemmaRMSNorm already elementwise); pi0.5 needs AdaRMSNorm swap; GR00T needs AdaLN swap. Native is the PyTorch-only A10G/H100 deployment target. ONNX-for-Jetson tracked separately per model (see *-onnx-parity goals below)."
    check: "test -f tests/test_native_parity.py && .venv/bin/python -m pytest tests/test_native_parity.py -q 2>/dev/null"
    weight: 9

  - id: smolvla-onnx-parity
    description: "End-to-end SmolVLA ONNX export produces cos >= 0.999 vs reference PyTorch predict_action_chunk on shared noise. Jetson customers deploy the ONNX path (not the PyTorch native path), so this is the real parity claim for Jetson-targeted customers. native-path-parity only verifies PyTorch. This goal verifies the ONNX path we actually ship."
    check: "test -f tests/test_smolvla_onnx_parity.py && .venv/bin/python -m pytest tests/test_smolvla_onnx_parity.py -q 2>/dev/null"
    weight: 9

  - id: pi0-onnx-parity
    description: "End-to-end pi0 ONNX export produces cos >= 0.999 vs reference PyTorch sample_actions on shared noise. Covers PaliGemma vision + Gemma language backbone + Gemma expert + flow matching loop. src/reflex/exporters/pi0_exporter.py (584 lines) exists but cos-parity has never been verified on shared-noise discipline. The cross-framework moat for pi0 — one of 4 models claimed to work."
    check: "test -f tests/test_pi0_onnx_parity.py && .venv/bin/python -m pytest tests/test_pi0_onnx_parity.py -q 2>/dev/null"
    weight: 9

  - id: pi05-onnx-parity
    description: "End-to-end pi0.5 ONNX export produces cos >= 0.999 vs reference PyTorch sample_actions. Requires AdaRMSNorm handling (DecomposedAdaRMSNorm exists in decompose.py). pi0_exporter.py export_pi05 is flagged 'not yet supported' pending this work."
    check: "test -f tests/test_pi05_onnx_parity.py && .venv/bin/python -m pytest tests/test_pi05_onnx_parity.py -q 2>/dev/null"
    weight: 7

  - id: gr00t-onnx-parity
    description: "End-to-end NVIDIA GR00T N1.6 ONNX export produces cos >= 0.999 vs reference PyTorch. Requires DiT + AdaLN (2-chunk scale+shift) handling. src/reflex/exporters/gr00t_exporter.py (756 lines) exists; cos-parity never verified under shared-noise discipline."
    check: "test -f tests/test_gr00t_onnx_parity.py && .venv/bin/python -m pytest tests/test_gr00t_onnx_parity.py -q 2>/dev/null"
    weight: 7

  - id: openvla-onnx-parity
    description: "OpenVLA (7.5B, tokenized action head, not flow matching) ONNX export + postprocess helper produces matching actions vs reference. Different path than the flow-matching VLAs — uses optimum-cli + our bin-to-continuous decoder. Parity verified with shared tokenized-action samples."
    check: "test -f tests/test_openvla_onnx_parity.py && .venv/bin/python -m pytest tests/test_openvla_onnx_parity.py -q 2>/dev/null"
    weight: 4

  - id: jetson-benchmark-ci
    description: "GitHub Action runs reflex bench on real Jetson hardware (CloudJetson or loaner) every commit, publishes ms/step per target to a public dashboard. Turns Leg B from static claim into per-commit proof."
    check: "test -f .github/workflows/jetson-bench.yml"
    weight: 9

  - id: ros2-bridge
    description: "reflex serve --ros2 wraps the HTTP endpoint with a thin rclpy action server for native ROS2 integration"
    check: "grep -q 'ros2' src/reflex/runtime/server.py 2>/dev/null || test -f src/reflex/runtime/ros2_bridge.py"
    weight: 8

  - id: calibration-metrics
    description: "reflex eval --calibration computes ECE + Brier + NLL on any lerobot dataset — the one non-sim metric with a monotonic link to real-task success (Zollo 2025). Differentiator vs other VLA deployment tools."
    check: "test -f src/reflex/eval/calibration.py && .venv/bin/python -c 'from reflex.eval.calibration import compute_ece, compute_brier, compute_nll' 2>/dev/null"
    weight: 8

  - id: prefix-kv-cache-reuse
    description: "reflex serve --cache-prefix reuses VLM prefix K/V across the 10-step denoise loop and multi-step rollouts. Dexmal 2510.26742 showed this unlocks 5-10x throughput for production trajectory serving."
    check: "grep -q 'prefix_cache\\|cache_prefix' src/reflex/runtime/server.py 2>/dev/null"
    weight: 8

  # ── MEDIUM-HIGH (weight 7) ────────────────────────────────────────

  - id: nan-guard-hardening
    description: "Guard rejects NaN/Inf actions and halts after N consecutive clamps (staleness kill-switch)"
    check: "grep -q 'nan' src/reflex/safety/guard.py 2>/dev/null && grep -q 'staleness\\|stale' src/reflex/safety/guard.py 2>/dev/null"
    weight: 7

  - id: xvla-exporter
    description: "reflex export auto-detects and exports xVLA (880M, tokenized action head) to ONNX"
    check: "test -f src/reflex/exporters/xvla_exporter.py && .venv/bin/python -c 'from reflex.exporters.xvla_exporter import export_xvla' 2>/dev/null"
    weight: 7

  - id: api-key-auth
    description: "reflex serve --api-key enables X-Reflex-Key header auth that rejects unauthenticated requests with 401"
    check: "grep -q 'api.key\\|api_key\\|X-Reflex-Key' src/reflex/runtime/server.py 2>/dev/null"
    weight: 7

  - id: action-chunk-buffering
    description: "reflex serve --replan-hz N --execute-hz M handles async replan-while-execute with a ring buffer. Real production pattern (Physical Intelligence uses it) — robots execute at 100Hz, VLAs plan at 20Hz."
    check: "grep -q 'replan_hz\\|sliding_window\\|action_buffer' src/reflex/runtime/server.py 2>/dev/null"
    weight: 7

  - id: docker-image-distribution
    description: "ghcr.io/rylinjames/reflex:<version>-<target> images published per release. Zero-install path: docker pull + docker run. Images for orin-nano, orin, thor, desktop. MVP-adoption tablestakes — first-hour experience goes from 4 hours of install to 60 seconds."
    check: "test -f .github/workflows/docker-publish.yml"
    weight: 7

  # ── MEDIUM (weight 5-6) ───────────────────────────────────────────

  - id: latency-histograms
    description: "/act response includes latency_p50, latency_p95, latency_p99, jitter_ms fields"
    check: "grep -q 'latency_p95\\|p95' src/reflex/runtime/server.py 2>/dev/null"
    weight: 6

  - id: stripe-license-gating
    description: "Stripe subscription verification gates Pro-tier features (distill, fleet batching) behind a valid license key. Revenue feature — not a moat piece, just a wrapper."
    check: "test -f src/reflex/licensing.py && .venv/bin/python -c 'from reflex.licensing import verify_license' 2>/dev/null"
    weight: 6

  - id: export-verification-report
    description: "Every reflex export auto-writes <export_dir>/VERIFICATION.md with per-stage cos, L2, max_abs, ONNX sizes, opset, target-hardware compat, provenance hash. 'Ship a receipt, not just a binary.'"
    check: "grep -q 'VERIFICATION.md\\|verification_report' src/reflex/exporters/*.py 2>/dev/null"
    weight: 6

  - id: quickstart-docs
    description: "README front page delivers 3-command quickstart (install / export / serve) that works on a fresh box in under 5 minutes. Plus docs/getting_started.md covering the 5 most common customer scenarios. MVP-adoption tablestakes."
    check: "test -f docs/getting_started.md && grep -q 'Quickstart' README.md && grep -c '```bash' docs/getting_started.md | grep -q '[3-9]'"
    weight: 6

  - id: adaptive-denoise-fix
    description: "Adaptive denoising works on pi0 (supported), is gated behind --experimental for smolvla/pi0.5/gr00t (unsafe)"
    check: "grep -q 'experimental\\|EXPERIMENTAL' src/reflex/kernels/turbo.py 2>/dev/null"
    weight: 5

  - id: determinism-version-hash
    description: "Every /act response includes model_hash, config_hash, reflex_version for reproducible debugging"
    check: "grep -q 'model_hash' src/reflex/runtime/server.py 2>/dev/null"
    weight: 5

  # ── LOW (weight 3-4) ──────────────────────────────────────────────

  - id: inference-test-coverage
    description: "Unit tests for inference.py and individual exporter modules (currently zero direct coverage)"
    check: "test -f tests/test_inference.py && .venv/bin/python -m pytest tests/test_inference.py -q --tb=no 2>/dev/null"
    weight: 4

  - id: openvla-exporter
    description: "reflex export auto-detects and exports OpenVLA (7.5B, tokenized head) beyond the current stub"
    check: "grep -c 'def export_openvla' src/reflex/exporters/openvla_exporter.py 2>/dev/null | grep -q '[1-9]'"
    weight: 4

  - id: sqlite-audit-log
    description: "reflex serve --audit-db appends every /act call to a SHA-256 hash-chain SQLite log"
    check: "test -f src/reflex/safety/audit_log.py && .venv/bin/python -c 'from reflex.safety.audit_log import AuditLog' 2>/dev/null"
    weight: 3

  # ── LAUNCH VERIFICATION (durable pre-launch + regression gates) ───

  - id: fresh-install-verified
    description: "`pip install 'reflex-vla[serve,gpu] @ git+https://...'` succeeds on a fresh python:3.12-slim container and `reflex --help` runs. Catches dep resolution breakage, broken editable installs, missing files in the wheel. Regression-check every release."
    check: "test -f tests/test_fresh_install.py && .venv/bin/python -m pytest tests/test_fresh_install.py -q 2>/dev/null"
    weight: 8

  - id: cuda-runtime-verified
    description: "Monolithic ONNX (SmolVLA + pi0) loads and runs on CUDAExecutionProvider with cos=1.0 vs CPUExecutionProvider on the same seeded inputs. Confirms the GPU serve path works end-to-end, not just the export. Without this, `reflex serve --device cuda` is a claim not a fact."
    check: "test -f tests/test_cuda_runtime_parity.py && .venv/bin/python -m pytest tests/test_cuda_runtime_parity.py -q 2>/dev/null"
    weight: 8

  - id: num-steps-quality-gate
    description: "Characterize action-quality drift between num_steps=1 (what monolithic ONNX currently bakes in) vs num_steps=10 (PyTorch default) on seeded fixtures. Write the cos/L2/max_abs drift to reflex_context/measured_numbers.md. If drift is significant, ship with warning + pin num_steps in model-card README. Without this, customers compare to their PyTorch num_steps=10 baseline and see regression they can't explain."
    check: "test -f tests/test_num_steps_quality.py && .venv/bin/python -m pytest tests/test_num_steps_quality.py -q 2>/dev/null"
    weight: 8

  - id: docker-image-smoke-test
    description: "Published `ghcr.io/rylinjames/reflex-vla:<version>` image runs end-to-end: `docker run --gpus all -v <export>:/exports -p 8000:8000 <image>` followed by `curl POST /act` returns a valid action chunk. Catches Dockerfile breakage, missing deps, entrypoint bugs — every release, before customers hit it."
    check: "test -f tests/test_docker_image_smoke.py && .venv/bin/python -m pytest tests/test_docker_image_smoke.py -q 2>/dev/null"
    weight: 7

  - id: ros2-bridge-live
    description: "`reflex ros2-serve` actually runs under real (not mocked) rclpy in a ros:humble container — subscribes to pub'd image/state/task topics, emits Float32MultiArray action chunks within the configured rate. Required because the current 8 tests use a FakeNode shim that will not catch rclpy API drift."
    check: "test -f tests/test_ros2_bridge_live.py && .venv/bin/python -m pytest tests/test_ros2_bridge_live.py -q 2>/dev/null"
    weight: 7

  - id: cli-export-end-to-end
    description: "`reflex export <hf_id> --output <dir>` from the installed CLI produces a working export directory: reflex_config.json + model.onnx (or the legal decomposed-stage files) + VERIFICATION.md. Verifies the customer's first command from the README actually works — not just the Modal export scripts. If the CLI still routes to an abandoned decomposed exporter, this catches it before launch."
    check: "test -f tests/test_cli_export_end_to_end.py && .venv/bin/python -m pytest tests/test_cli_export_end_to_end.py -q 2>/dev/null"
    weight: 9

  - id: serve-act-roundtrip
    description: "`reflex serve <export_dir>` boots and an HTTP POST /act with image+instruction+state returns a valid action chunk (shape [chunk, action_dim], all-finite). Closes the loop between export + server code — we have ONNX parity but have never actually served a request end-to-end through the FastAPI layer."
    check: "test -f tests/test_serve_act_roundtrip.py && .venv/bin/python -m pytest tests/test_serve_act_roundtrip.py -q 2>/dev/null"
    weight: 9

  - id: runtime-num-steps-10
    description: "Reflex runtime (`ReflexServer` / `Pi0OnnxServer` / `SmolVLANativeServer`) correctly serves the num_steps=10 monolithic ONNX. Current servers were written assuming num_steps=1; need to verify the num_steps=10 artifact's I/O contract (inputs, action chunk shape, noise handling) is compatible or the runtime is updated."
    check: "test -f tests/test_runtime_num_steps_10.py && .venv/bin/python -m pytest tests/test_runtime_num_steps_10.py -q 2>/dev/null"
    weight: 8

  - id: guard-trip-integration
    description: "ActionGuard kill-switch integration: when max_consecutive_clamps is hit in a live `reflex serve`, POST /act returns the `guard_tripped` error (not actions), and POST /guard/reset clears the state. Verifies the full path from unit-tested guard → live HTTP behavior."
    check: "test -f tests/test_guard_trip_integration.py && .venv/bin/python -m pytest tests/test_guard_trip_integration.py -q 2>/dev/null"
    weight: 7

  # ── REGRESSION GATE (runs after every /evolve cycle) ──────────────

  - id: native-path-parity
    description: "Native export path (lerobot SmolVLAPolicy + DecomposedRMSNorm) matches PyTorch reference to cos=1.0000 end-to-end. The one load-bearing number for reflex's deployment-parity claim."
    check: "test -f src/reflex/runtime/smolvla_native.py && test -f scripts/local_full_diff.py && REFLEX_NATIVE=1 .venv/bin/python scripts/local_full_diff.py 2>&1 | grep -q 'cos=+1.000'"
    weight: 10

  # ── MONETIZATION BLOCKERS (v0.2.1 → v0.3 ship list) ───────────────
  # Added 2026-04-19 after gap analysis. Each of these is something a
  # paid customer would demand before handing over $500-$2,000/mo.
  # v0.2 ships the "tech is verified" story; v0.3 must ship the "tech
  # actually works for your robot on your hardware" story.

  - id: task-success-benchmark
    description: "Publish task-success numbers for the monolithic ONNX path on at least ONE benchmark (LIBERO-10, SimplerEnv, or real-robot task). cos=1.0 parity verifies ONNX matches PyTorch mathematically — NOT that the robot completes the task. Without task-success numbers, a buyer reading cos=1.0 assumes deployment = working, which is an empirical gap. Blocks paid monetization: no customer pays $2k/mo for 'verified parity, task success unmeasured.' Prior LIBERO-10 runs on the decomposed path hit 0% (see 05_sessions/2026-04-17_libero_correctness_hunt.md); need to re-run on the cos=1.0 monolithic path."
    check: "test -f reflex_context/06_experiments/task_success_results.md && grep -q 'LIBERO\\|SimplerEnv\\|success_rate' reflex_context/06_experiments/task_success_results.md 2>/dev/null"
    weight: 10

  - id: jetson-latency-published
    description: "Publish real ms/step latency on Jetson Orin Nano + Orin + Thor for each supported VLA (SmolVLA, pi0, pi0.5, GR00T). Per-model × per-target matrix with p50/p95/p99. The entire product pitch is 'deploy to Jetson' — without these numbers a buyer can't size hardware, budget, or timing loop. Blocks on hardware access (CloudJetson Orin Nano waitlisted); unblock via Amazon-return dev kit, community bounty, or first customer's hardware. Related: `jetson-benchmark-ci` sets up the CI infrastructure; this goal ensures numbers actually get published."
    check: "test -f reflex_context/06_experiments/jetson_latency_results.md && grep -q 'ms/step\\|p50\\|p95' reflex_context/06_experiments/jetson_latency_results.md 2>/dev/null"
    weight: 10

  - id: gr00t-vlm-conditioning
    description: "Export Eagle-2-HG VLM backbone as a separate ONNX, chain its KV output into the GR00T DiT per denoise step. Currently the GR00T monolithic ONNX uses a ZERO VLM-KV placeholder — DiT + AdaLN stack is cos=1.0 machine precision, but without real VLM conditioning the exported artifact produces actions that are a pure function of noise. 'GR00T support' in the README is technically true but not deployable for actual multi-modal control. Blocks paid monetization on any customer who wants GR00T (NVIDIA-backed, heaviest marketing tailwind of the four VLAs)."
    check: "test -f src/reflex/exporters/gr00t_vlm_exporter.py && .venv/bin/python -c 'from reflex.exporters.gr00t_vlm_exporter import export_gr00t_with_vlm' 2>/dev/null"
    weight: 9

  - id: orin-nano-fp16-fit
    description: "pi0 (12.5 GB) and pi0.5 (13 GB) monolithic ONNX don't fit on Orin Nano 8GB at FP32. FP16 engine rebuild + Orin Nano fit validation so pi0/pi0.5 actually run on the entry-level Jetson. Without this, the 'pick up a $499 Orin Nano and deploy pi0' story in our pitch is literally false for the most common VLA model. SmolVLA (1.6GB) and GR00T (4.4GB) already fit; this is a pi-family-specific fit problem. Requires TRT FP16 engine build with validated parity loss < 1% vs FP32."
    check: "test -f tests/test_orin_nano_fp16_fit.py && .venv/bin/python -m pytest tests/test_orin_nano_fp16_fit.py -q 2>/dev/null"
    weight: 8

  - id: docker-arm64-jetson
    description: "Native arm64 Docker image (`ghcr.io/rylinjames/reflex-vla:<version>-arm64` or multi-arch manifest) that runs on Jetson hardware. Current published image is x86_64 only; Jetson users must build from source. The 'zero-install Docker run' quickstart in our README works on x86 GPUs (A10G, RTX) but is a dead-end for the primary customer target. Requires: QEMU emulation or native arm64 build in GitHub Actions + Jetson-specific CUDA/cuDNN pinning + smoke test on real Jetson hardware."
    check: "test -f .github/workflows/docker-publish-arm64.yml && grep -q 'arm64\\|linux/arm64' .github/workflows/docker-publish-arm64.yml 2>/dev/null"
    weight: 7

  - id: fine-tuning-pipeline
    description: "EXPLICIT SCOPE EXPANSION (2026-04-19): buyers often conflate 'deployment' with 'training' — they want an end-to-end workflow from their dataset to a deployed ONNX. Current reflex scope is deployment-only (PyTorch/JAX owns training per the product positioning). This goal adds a thin fine-tuning wrapper: `reflex finetune --base lerobot/smolvla_base --dataset <hf_id> --output ./my_export` that wraps lerobot's existing training loops + auto-exports the fine-tuned checkpoint. Lower weight than the other monetization blockers because it's a positioning question (is reflex 'the deploy tool' or 'the end-to-end tool?') — consider whether to build it before deciding to charge for it."
    check: "test -f src/reflex/finetune.py && .venv/bin/python -c 'from reflex.finetune import run_finetune' 2>/dev/null"
    weight: 5

  # ── PHASE 2 LIBERO (added 2026-04-19 after first LIBERO-10 run = 0/10) ──
  # The initial LIBERO harness runs end-to-end (infra win) but 0% task
  # success — root cause hypothesis: adapter runs with vlm=off. These
  # three goals are the staged fix path. See
  # reflex_context/06_experiments/task_success_results.md.

  - id: vla-eval-adapter-vlm-on
    description: "Turn on VLM conditioning in src/reflex/runtime/adapters/vla_eval.py. Currently runs with vlm=off — adapter feeds images + state + dummy language to the expert. The SmolVLA LIBERO fine-tune was trained with full VLM-conditioned tokens; without it, the model can't resolve task intent ('alphabet soup' vs 'butter'). Confirm the fix by re-running `modal run scripts/modal_libero_monolithic.py` and expecting a non-zero task-success number. Fastest signal path to validating the hypothesis."
    check: "test -f reflex_context/06_experiments/task_success_results.md && grep -q 'vlm=on\\|Phase 2 result\\|LIBERO-10 non-zero' reflex_context/06_experiments/task_success_results.md 2>/dev/null"
    weight: 10

  - id: libero-via-monolithic-onnx
    description: "Extend vla_eval.py to route through SmolVLAOnnxServer (monolithic cos=+1.000000 path) when a flag is set (e.g. REFLEX_MONOLITHIC=1). Currently LIBERO uses REFLEX_NATIVE=1 (PyTorch). The monolithic ONNX is what customers actually ship, so the task-success number on the monolithic path is the load-bearing customer claim. Depends on vla-eval-adapter-vlm-on producing a non-zero baseline first — otherwise we can't distinguish adapter-plumbing bugs from monolithic-path bugs."
    check: "grep -q 'SmolVLAOnnxServer\\|REFLEX_MONOLITHIC' src/reflex/runtime/adapters/vla_eval.py 2>/dev/null"
    weight: 9

  - id: libero-preprocessing-audit
    description: "Contingency goal: if vla-eval-adapter-vlm-on is shipped but LIBERO-10 still yields 0% (or very low), something deeper is off. Audit: (a) state normalization (what values does LIBERO emit vs what the fine-tune was trained on?), (b) image preprocessing + camera keying (LIBERO has agentview + eye_in_hand; adapter currently picks 'first camera' — may be wrong), (c) action chunk unnormalization, (d) action chunk chunk_size vs LIBERO timestep cadence. Result: a documented per-dimension trace comparing adapter inputs/outputs vs the SmolVLA LIBERO fine-tune's published training config."
    check: "test -f reflex_context/06_experiments/libero_preprocessing_audit.md"
    weight: 7

  - id: libero-adapter-lerobot-conformance
    description: "DISCOVERED 2026-04-19 after pipeline-clean parity test: our adapter vs raw SmolVLAPolicy match bit-exact (cos=+1.000000, max_abs=0.0), so the LIBERO 0% isn't a pipeline bug. Web research identified FIVE concrete deltas between our harness and lerobot's own LIBERO eval: (1) MODEL — we use community checkpoint `lerobot/smolvla_libero` (471 dl, no published eval); switch to canonical `HuggingFaceVLA/smolvla_libero` (11k dl, paper libero_10=71%). (2) 180° H+W image flip — lerobot's LiberoProcessorStep does torch.flip(img, dims=[2,3]) on every camera image; our adapter doesn't. 'Almost certainly alone drops success to near-zero.' (3) CAMERA KEYS — canonical config expects observation.images.image + image2 (2 cameras); our adapter uses camera1/2/3 with a phantom 3rd replica. (4) n_action_steps=1 — the paper's eval re-queries every env step (throws away 49/50 predicted actions per chunk); vla-eval may be running open-loop 50-step chunks. (5) STATE = 8-dim eef_pos(3) + quat-to-axis-angle(3) + gripper_qpos(2) per LiberoProcessorStep formula. All 5 fixes together should move the needle from 0% toward the paper's 71%. Each is independently implementable."
    check: "test -f reflex_context/06_experiments/libero_lerobot_conformance.md && grep -q 'flip\\|n_action_steps\\|HuggingFaceVLA' reflex_context/06_experiments/libero_lerobot_conformance.md 2>/dev/null"
    weight: 10
